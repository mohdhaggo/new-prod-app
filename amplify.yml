version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step required for static site"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
